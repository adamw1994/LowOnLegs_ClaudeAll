## Project Context
- This is a TypeScript scoreboard app
- Run `npm run build` and `npm run lint` after multi-file changes
- Commit in logical chunks rather than one large commit to avoid losing work on rate limits
